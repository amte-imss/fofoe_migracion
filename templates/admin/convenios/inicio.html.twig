{% extends 'layouts/app.html.twig' %}

{% block title %}Convenios - {{ title }}{% endblock %}

{% block body %}
<div class="container-xl py-4">
    <div class="page-header mb-4">
        <h2 class="page-title">{{ title }}</h2>
        <div class="text-muted mt-1">{{ description }}</div>
    </div>

    <div class="card">
        <div class="card-body">
            <h3 class="card-title">Pantalla de inicio</h3>
            <p class="text-muted mb-0">Pendiente de migrar la funcionalidad completa de esta seccion.</p>
        </div>
    </div>
</div>
{% endblock %}
