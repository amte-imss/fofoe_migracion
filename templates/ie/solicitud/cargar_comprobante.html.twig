{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('ie.cargar.comprobante') }}
{% endblock %}

{% block body %}
    {{ render(controller('App\\Controller\\IE\\InstitucionController::menu')) }}

    <div class="section-md">
        <div class='container-fluid'>
            <div class='container'>
                <h1 class="text-bold text-center">Registrar comprobante de pago</h1>
                <div style="display: flex;">
                    <p class="text-bold mt-10">Solicitud {{ solicitud.noSolicitud }}</p>
                </div>
                <div class="row">
                    <div class="col-md-12">
                        <div id="cargar-comprobante-component"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('ie.cargar.comprobante') }}
    <script>
        window.RFC_PROP      = {{ institucion | json_encode | raw }};
        window.SOLICITUD_PROP = {{ solicitud  | json_encode | raw }};
    </script>
{% endblock %}
